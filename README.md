# SSH-like-Certificate-Authority-Service
Users request a temporary signature (static token) via an HTTP API, allowing access to any server that trusts the CA’s Master Key (and there is no server that trusts my Master Key so you can't access anything with this).
